---
phase: 03-operational-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - .dockerignore
  - docker-compose.yml
  - .github/workflows/docker-publish.yml
autonomous: false
requirements:
  - OPS-01
must_haves:
  truths:
    - "docker build . completes without error"
    - "Docker image starts and responds to GET /health with 200"
    - "Container timezone is America/Sao_Paulo (TZ enforced)"
    - "Production image contains only dist/ and prod dependencies — no devDependencies"
    - "GitHub Actions workflow triggers on push to main and pushes image to ghcr.io"
    - ".env file is excluded from the Docker build context"
  artifacts:
    - path: "Dockerfile"
      provides: "Multi-stage build: builder (node:22-slim) + runner (node:22-slim)"
      contains: "FROM node:22-slim AS builder"
    - path: ".dockerignore"
      provides: "Excludes node_modules, .env, .git, dist, .planning from build context"
      contains: ".env"
    - path: "docker-compose.yml"
      provides: "Service definition with env_file, restart:always, HEALTHCHECK"
      contains: "restart: always"
    - path: ".github/workflows/docker-publish.yml"
      provides: "CI workflow: build + push to ghcr.io on push to main"
      contains: "ghcr.io"
  key_links:
    - from: "Dockerfile runner stage"
      to: "TZ=America/Sao_Paulo"
      via: "ENV directive"
      pattern: "TZ=America/Sao_Paulo"
    - from: "docker-compose.yml HEALTHCHECK"
      to: "GET /health"
      via: "node -e http.get inline (no curl needed)"
      pattern: "health"
    - from: ".github/workflows/docker-publish.yml"
      to: "ghcr.io/${{ github.repository }}"
      via: "docker/metadata-action@v5 + docker/build-push-action@v6"
      pattern: "ghcr.io"
---

<objective>
Create the Docker packaging artifacts and GitHub Actions CI/CD workflow. This enables
the service to run in a production container with correct timezone, and automates image
publishing on every push to main.

Purpose: OPS-01 — application runs inside a Docker container.
Output: Dockerfile, .dockerignore, docker-compose.yml, .github/workflows/docker-publish.yml.
No code changes to src/ — purely infrastructure files.
</objective>

<execution_context>
@/root/time-trigger-api/.claude/get-shit-done/workflows/execute-plan.md
@/root/time-trigger-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-operational-hardening/03-CONTEXT.md
@.planning/phases/03-operational-hardening/03-RESEARCH.md

<interfaces>
<!-- Key facts extracted from research and existing codebase. -->

From src/main.ts (already handles PORT):
```typescript
await app.listen(process.env['PORT'] ?? 3000);
// No change needed — PORT env var already supported.
```

From nest-cli.json (confirmed entrypoint):
```
entryFile: "main" → compiled output is dist/main.js
```

From package.json (pnpm lockfile present):
```
pnpm-lock.yaml exists → use pnpm install --frozen-lockfile
```

Key decisions locked in CONTEXT.md:
- D-01: Base image node:22-slim
- D-02: Multi-stage build (builder → runner)
- D-03: TZ=America/Sao_Paulo in Dockerfile ENV
- D-04: docker-compose.yml with env_file, restart:always, HEALTHCHECK
- D-05: .dockerignore excludes node_modules, .planning, .git
- D-06: Health response { status: 'ok', uptime } — Docker HEALTHCHECK only needs HTTP 200
- D-07: PORT via env var (already in main.ts)
- D-08: HEALTHCHECK uses curl — BUT pitfall: node:22-slim has no curl. Use node -e inline instead.
- D-12/13/14: GitHub Actions on push to main, push to ghcr.io

PITFALL (from 03-RESEARCH.md): node:22-slim does NOT include curl.
Solution: Use node -e inline HEALTHCHECK instead of curl (avoids apt-get layer).

PITFALL: BuildKit cache mount (--mount=type=cache) requires BuildKit.
For local docker-compose: document DOCKER_BUILDKIT=1 in comment.
For GitHub Actions: docker/setup-buildx-action enables BuildKit automatically.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dockerfile, .dockerignore, and docker-compose.yml</name>
  <files>Dockerfile, .dockerignore, docker-compose.yml</files>

  <read_first>
    - src/main.ts — confirm PORT handling (process.env['PORT'] ?? 3000)
    - nest-cli.json — confirm entryFile is "main" → dist/main.js
    - package.json — confirm pnpm-lock.yaml exists and check for "packageManager" field
    - .planning/phases/03-operational-hardening/03-RESEARCH.md §Pattern 3 and §Pattern 4 — canonical Dockerfile and docker-compose patterns
  </read_first>

  <action>
Create Dockerfile at project root:

```dockerfile
# ---- Stage 1: builder ----
FROM node:22-slim AS builder

ENV PNPM_HOME="/pnpm"
ENV PATH="$PNPM_HOME:$PATH"
RUN corepack enable

WORKDIR /app

# Copy lockfile + manifests first — maximises layer cache hits
COPY package.json pnpm-lock.yaml ./

# Install ALL deps (including devDependencies) for TypeScript build
RUN --mount=type=cache,id=pnpm,target=/pnpm/store pnpm install --frozen-lockfile

# Copy source and build
COPY . .
RUN pnpm run build

# ---- Stage 2: runner ----
FROM node:22-slim AS runner

ENV PNPM_HOME="/pnpm"
ENV PATH="$PNPM_HOME:$PATH"
ENV NODE_ENV=production
ENV TZ=America/Sao_Paulo

RUN corepack enable

WORKDIR /app

COPY package.json pnpm-lock.yaml ./

# Install production dependencies only
RUN --mount=type=cache,id=pnpm,target=/pnpm/store pnpm install --frozen-lockfile --prod

COPY --from=builder /app/dist ./dist

CMD ["node", "dist/main.js"]
```

Create .dockerignore at project root:

```
node_modules
dist
.git
.github
.planning
coverage
*.log
.env
.env.*
```

Create docker-compose.yml at project root:

```yaml
version: '3.8'
services:
  time-trigger-api:
    build: .
    # For pre-built image from ghcr.io, replace 'build: .' with:
    # image: ghcr.io/<your-github-username>/time-trigger-api:latest
    env_file: .env
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    restart: always
    healthcheck:
      # Uses node built-in http — no curl needed (node:22-slim has no curl)
      test:
        - "CMD-SHELL"
        - "node -e \"require('http').get('http://localhost:${PORT:-3000}/health', r => process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))\""
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
```

Note: --mount=type=cache requires BuildKit. For local builds use:
  DOCKER_BUILDKIT=1 docker-compose build
GitHub Actions uses docker/setup-buildx-action which enables BuildKit automatically.

Commit conventions (per CLAUDE.md):
- Use `🏗️ build: add multi-stage Dockerfile and docker-compose.yml`
- NO Co-Authored-By footer
  </action>

  <verify>
    <automated>docker build -t time-trigger-api-test . 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - grep -n "FROM node:22-slim AS builder" Dockerfile → line exists (D-01, D-02)
    - grep -n "TZ=America/Sao_Paulo" Dockerfile → line exists (D-03)
    - grep -n "pnpm install --frozen-lockfile --prod" Dockerfile → line exists (runner prod deps only)
    - grep -n "COPY --from=builder /app/dist ./dist" Dockerfile → line exists
    - grep -n "CMD \[\"node\", \"dist/main.js\"\]" Dockerfile → line exists
    - grep -n "\.env$" .dockerignore → line exists (security: .env excluded)
    - grep -n "node_modules" .dockerignore → line exists
    - grep -n "restart: always" docker-compose.yml → line exists (D-04)
    - grep -n "env_file" docker-compose.yml → line exists (D-04)
    - grep -n "health" docker-compose.yml → HEALTHCHECK line exists (D-04)
    - docker build -t time-trigger-api-test . exits 0
  </acceptance_criteria>

  <done>
    Dockerfile builds successfully with multi-stage pattern.
    .dockerignore excludes .env and other non-production files.
    docker-compose.yml defines service with restart:always and node-based HEALTHCHECK.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions CI/CD workflow for ghcr.io publish</name>
  <files>.github/workflows/docker-publish.yml</files>

  <read_first>
    - .planning/phases/03-operational-hardening/03-RESEARCH.md §Pattern 5 — verified GitHub Actions workflow YAML
    - .gitignore — confirm .github/ is not excluded
  </read_first>

  <action>
Create .github/workflows/ directories if they do not exist, then create docker-publish.yml:

```yaml
name: Build and publish Docker image

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=sha
            type=ref,event=branch
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
```

Key implementation notes (per CLAUDE.md and research):
- `permissions: packages: write` is MANDATORY — without it push fails with 403 (Pitfall 5)
- `docker/metadata-action@v5` lowercases the repository name automatically (handles ghcr.io case sensitivity)
- `secrets.GITHUB_TOKEN` is auto-available — no manual secret creation needed
- `type=raw,value=latest,enable={{is_default_branch}}` tags latest only on main pushes
- `cache-from: type=gha` uses GHA cache API v2 (current standard post-April 2025)

Commit conventions (per CLAUDE.md):
- Use `🤖 ci: add GitHub Actions workflow to build and push Docker image to ghcr.io`
- NO Co-Authored-By footer
  </action>

  <verify>
    <automated>cat .github/workflows/docker-publish.yml | grep -c "packages: write"</automated>
  </verify>

  <acceptance_criteria>
    - grep -n "packages: write" .github/workflows/docker-publish.yml → line exists (Pitfall 5)
    - grep -n "ghcr.io" .github/workflows/docker-publish.yml → 2+ lines exist
    - grep -n "docker/build-push-action@v6" .github/workflows/docker-publish.yml → line exists
    - grep -n "cache-from: type=gha" .github/workflows/docker-publish.yml → line exists
    - grep -n "actions/checkout@v4" .github/workflows/docker-publish.yml → line exists (not v3)
    - grep -n "branches:" .github/workflows/docker-publish.yml → line exists (triggers on push to main)
    - cat .github/workflows/docker-publish.yml exits 0 (file is readable/exists)
  </acceptance_criteria>

  <done>
    .github/workflows/docker-publish.yml created with correct permissions, ghcr.io login,
    metadata-action tagging, and GHA layer cache. Workflow triggers on push to main.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Multi-stage Dockerfile (node:22-slim, TZ=America/Sao_Paulo, pnpm corepack),
    .dockerignore (excludes .env, node_modules, .git, .planning),
    docker-compose.yml (restart:always, env_file:.env, node-based HEALTHCHECK),
    .github/workflows/docker-publish.yml (ghcr.io push on main, GHA cache).
  </what-built>
  <how-to-verify>
    1. Build the image locally:
       DOCKER_BUILDKIT=1 docker build -t time-trigger-api .
       Expected: Build completes, two stages visible in output (builder → runner).

    2. Verify image size is reasonable (should be under 300MB):
       docker images time-trigger-api

    3. Start the container with a minimal env (no real MongoDB needed — health check only):
       docker run -d -p 3000:3000 -e MONGODB_URI=mongodb://localhost:27017 -e CRON_INTERVAL=60000 -e TZ=America/Sao_Paulo --name test-api time-trigger-api
       Wait 5 seconds, then:
       curl http://localhost:3000/health
       Expected response: {"status":"ok","uptime":...} with HTTP 200.

    4. Verify timezone inside container:
       docker exec test-api node -e "console.log(Intl.DateTimeFormat().resolvedOptions().timeZone)"
       Expected: America/Sao_Paulo

    5. Clean up:
       docker stop test-api && docker rm test-api

    6. Verify .env is NOT in the Docker build context:
       grep ".env$" .dockerignore
       Expected: line exists
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. docker build -t time-trigger-api . → exits 0
2. curl http://localhost:3000/health → { "status": "ok", "uptime": N }
3. grep -n "TZ=America/Sao_Paulo" Dockerfile → line exists
4. grep -n "packages: write" .github/workflows/docker-publish.yml → line exists
5. grep -n "\.env$" .dockerignore → line exists
</verification>

<success_criteria>
- Dockerfile builds without errors using multi-stage node:22-slim pattern
- Runner stage contains only dist/ and prod node_modules
- TZ=America/Sao_Paulo set in runner stage ENV
- .dockerignore excludes .env (security) and node_modules, .git, .planning
- docker-compose.yml has restart:always, env_file:.env, working HEALTHCHECK
- GitHub Actions workflow: packages:write permission, ghcr.io push, GHA cache, triggers on main
- Human verification passes: container starts, GET /health returns 200, timezone confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/03-operational-hardening/03-03-SUMMARY.md`
</output>
