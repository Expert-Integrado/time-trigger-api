---
phase: 05-per-client-time-controls
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/dispatch/run-dispatch.service.ts
  - src/dispatch/run-dispatch.service.spec.ts
autonomous: true
requirements: [TRIG-01, TRIG-02, TRIG-03, TRIG-04, TRIG-05, TRIG-06]

must_haves:
  truths:
    - "Banco sem campo timeTrigger no vars é ignorado — zero runs despachados"
    - "Banco com timeTrigger.enabled: false é ignorado — zero runs despachados"
    - "Runs só são despachados dentro da janela morningLimit–nightLimit do timeTrigger"
    - "Runs só são despachados em dias presentes em timeTrigger.allowedDays"
    - "Campos root-level morningLimit/nightLimit não são mais lidos"
  artifacts:
    - path: "src/dispatch/run-dispatch.service.ts"
      provides: "Lógica de processDatabase refatorada para usar timeTrigger"
      contains: "timeTrigger"
    - path: "src/dispatch/run-dispatch.service.spec.ts"
      provides: "Testes TDD cobrindo os 6 requisitos TRIG"
      contains: "TRIG-01"
  key_links:
    - from: "src/dispatch/run-dispatch.service.ts"
      to: "vars.timeTrigger"
      via: "findOne<VarsDoc> no MongoDB"
      pattern: "timeTrigger"
    - from: "processDatabase"
      to: "isWithinTimeWindow"
      via: "timeTrigger.morningLimit e timeTrigger.nightLimit"
      pattern: "timeTrigger\\.morningLimit"
    - from: "processDatabase"
      to: "isAllowedDay"
      via: "timeTrigger.allowedDays e new Date().getDay()"
      pattern: "allowedDays"
---

<objective>
Refatorar RunDispatchService.processDatabase() para usar o objeto timeTrigger do vars de cada banco, substituindo os campos root-level morningLimit/nightLimit por timeTrigger.morningLimit, timeTrigger.nightLimit e adicionando o gate de dia da semana via timeTrigger.allowedDays.

Purpose: Dar controle per-client sobre quando o Time Trigger processa runs — cada banco de dados pode habilitar/desabilitar independentemente e configurar sua própria janela de horário e dias permitidos.

Output: run-dispatch.service.ts refatorado + testes cobrindo TRIG-01 a TRIG-06 passando.
</objective>

<execution_context>
@/root/time-trigger-api/.claude/get-shit-done/workflows/execute-plan.md
@/root/time-trigger-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/vars-schema.md

<interfaces>
<!-- Contrato atual em run-dispatch.service.ts (será substituído) -->

Interface VarsDoc ATUAL (remover após refactor):
```typescript
interface VarsDoc {
  morningLimit?: number;
  nightLimit?: number;
}
```

Interface VarsDoc NOVA (implementar):
```typescript
interface TimeTriggerConfig {
  enabled: boolean;
  morningLimit: number;
  nightLimit: number;
  allowedDays: number[]; // 0=Domingo...6=Sábado
}

interface VarsDoc {
  timeTrigger?: TimeTriggerConfig;
}
```

Método isWithinTimeWindow ATUAL (assinatura que muda):
```typescript
private isWithinTimeWindow(morningLimit: number, nightLimit: number): boolean
```

Métodos NOVOS a criar:
```typescript
private isWithinTimeWindow(morningLimit: number, nightLimit: number): boolean
// mantém mesma lógica: currentHour >= morning && currentHour < night

private isAllowedDay(allowedDays: number[]): boolean
// new Date().getDay() retorna 0-6; retorna allowedDays.includes(dayOfWeek)
```

Comportamento do processDatabase refatorado (ordem das verificações):
1. Ler vars com findOne<VarsDoc>({})
2. Se vars?.timeTrigger não existir → log warn + return (TRIG-01, TRIG-02)
3. Se timeTrigger.enabled === false → log warn + return (TRIG-03)
4. Se !isWithinTimeWindow(timeTrigger.morningLimit, timeTrigger.nightLimit) → return (TRIG-04)
5. Se !isAllowedDay(timeTrigger.allowedDays) → return (TRIG-06)
6. Ler webhooks e despachar runs (comportamento existente)
</interfaces>
</context>

<feature>
  <name>timeTrigger per-client controls em RunDispatchService</name>
  <files>src/dispatch/run-dispatch.service.ts, src/dispatch/run-dispatch.service.spec.ts</files>
  <behavior>
    TRIG-01/TRIG-02 — vars sem timeTrigger: banco ignorado
    - Input: vars = null → skip, zero dispatches
    - Input: vars = { botIdentifier: 'x' } (sem timeTrigger) → skip, zero dispatches
    - Input: vars = { timeTrigger: { enabled: true, morningLimit: 8, nightLimit: 20, allowedDays: [1,2,3,4,5] } } → processa

    TRIG-03 — timeTrigger.enabled: false: banco ignorado
    - Input: vars.timeTrigger = { enabled: false, ... } → skip, zero dispatches

    TRIG-04 — time gate via timeTrigger.morningLimit/nightLimit (NÃO root-level)
    - currentHour=6, morningLimit=8 → skip
    - currentHour=23, nightLimit=20 → skip
    - currentHour=10, morningLimit=8, nightLimit=20 → processa

    TRIG-05/TRIG-06 — gate de dia da semana via allowedDays
    - allowedDays=[1,2,3,4,5], currentDay=0 (domingo) → skip
    - allowedDays=[1,2,3,4,5], currentDay=1 (segunda) → processa
    - allowedDays=[0,1,2,3,4,5,6], currentDay=6 → processa (todos os dias)

    Regressão — testes existentes ainda passam:
    - DETECT-01: query runs com runStatus:waiting e waitUntil <= now
    - DETECT-02: vars lido a cada cycle (sem cache)
    - DETECT-03: webhooks lido a cada cycle (sem cache)
    - SCHED-03: isRunning guard funciona
    - CONN-06: banco com erro não bloqueia outros bancos
  </behavior>
  <implementation>
    Ciclo RED→GREEN→REFACTOR:

    **FASE RED — Escrever testes que falham primeiro:**

    1. Ler run-dispatch.service.spec.ts (arquivo atual completo)
    2. Atualizar run-dispatch.service.spec.ts:
       a. Substituir `withinWindowVars = { morningLimit: 8, nightLimit: 22 }` por:
          `withinWindowVars = { timeTrigger: { enabled: true, morningLimit: 8, nightLimit: 22, allowedDays: [0,1,2,3,4,5,6] } }`
       b. Adicionar `const allowedDay = 1; // segunda-feira` para usar nos testes
       c. Atualizar TODOS os testes existentes que usam `withinWindowVars` para que mock de getHours retorne 10 AND mock de getDay retorne `allowedDay` (1)
       d. Corrigir o teste que verifica warn com 'morningLimit' — mudar para verificar 'timeTrigger'
       e. Adicionar novos testes:
          - `(TRIG-01) skips when timeTrigger field is absent from vars` — vars={botIdentifier:'x'}, zero dispatch
          - `(TRIG-02) skips when vars document is null` — vars=null, zero dispatch
          - `(TRIG-03) skips when timeTrigger.enabled is false` — enabled:false, zero dispatch
          - `(TRIG-04) skips when currentHour < timeTrigger.morningLimit` — getHours=6, morningLimit=8, zero dispatch
          - `(TRIG-04) skips when currentHour >= timeTrigger.nightLimit` — getHours=23, nightLimit=20, zero dispatch
          - `(TRIG-04) dispatches when currentHour within window` — getHours=10, dispatch 1 vez
          - `(TRIG-06) skips when currentDay not in allowedDays` — getDay=0, allowedDays=[1,2,3,4,5], zero dispatch
          - `(TRIG-05/TRIG-06) dispatches when currentDay in allowedDays` — getDay=1, allowedDays=[1,2,3,4,5], dispatch 1 vez
       f. Confirmar que testes falham: `pnpm test -- --testPathPattern=run-dispatch`

    **FASE GREEN — Implementar para os testes passarem:**

    1. Ler run-dispatch.service.ts (arquivo atual completo)
    2. Substituir interface VarsDoc:
       ```typescript
       interface TimeTriggerConfig {
         enabled: boolean;
         morningLimit: number;
         nightLimit: number;
         allowedDays: number[];
       }

       interface VarsDoc {
         timeTrigger?: TimeTriggerConfig;
       }
       ```
    3. Reescrever processDatabase():
       ```typescript
       private async processDatabase(dbName: string): Promise<void> {
         const db: Db = this.mongoService.db(dbName);

         // TRIG-01, TRIG-02: fresh vars read; timeTrigger obrigatório
         const vars = await db.collection('vars').findOne<VarsDoc>({});
         if (!vars?.timeTrigger) {
           this.logger.warn(
             `[${dbName}] timeTrigger not found in vars — skipping`,
           );
           return;
         }

         // TRIG-03: enabled flag
         if (!vars.timeTrigger.enabled) {
           this.logger.warn(
             `[${dbName}] timeTrigger.enabled is false — skipping`,
           );
           return;
         }

         // TRIG-04: time gate (TZ=America/Sao_Paulo makes getHours() return Brazil time)
         if (!this.isWithinTimeWindow(vars.timeTrigger.morningLimit, vars.timeTrigger.nightLimit)) {
           return;
         }

         // TRIG-05, TRIG-06: day-of-week gate
         if (!this.isAllowedDay(vars.timeTrigger.allowedDays)) {
           return;
         }

         // DETECT-03: fresh webhooks read every cycle
         const webhookDoc = await db.collection('webhooks').findOne<WebhookDoc>({});
         const webhookUrl = webhookDoc?.['Processador de Runs'];
         if (!webhookUrl) {
           this.logger.warn(
             `[${dbName}] "Processador de Runs" URL missing from webhooks — skipping`,
           );
           return;
         }

         // DETECT-01: find waiting runs with waitUntil in the past
         const runs: Document[] = await db
           .collection('runs')
           .find({ runStatus: 'waiting', waitUntil: { $lte: new Date() } })
           .toArray();

         for (const run of runs) {
           await this.webhookDispatchService.dispatch(db, run, webhookUrl);
         }
       }
       ```
    4. Adicionar método isAllowedDay() após isWithinTimeWindow():
       ```typescript
       private isAllowedDay(allowedDays: number[]): boolean {
         const currentDay = new Date().getDay(); // Brazil time due to TZ env
         return allowedDays.includes(currentDay);
       }
       ```
    5. Manter isWithinTimeWindow() inalterado (mesma assinatura e lógica)
    6. Rodar testes: `pnpm test -- --testPathPattern=run-dispatch`
    7. Confirmar que TODOS os testes passam (novos + regressão)

    **FASE REFACTOR — Limpar se necessário:**
    - Verificar se há comentários desatualizados (ex: referência a DETECT-02 no vars read — atualizar para TRIG-01/TRIG-02)
    - Rodar lint: `pnpm run lint`
    - Rodar testes finais: `pnpm test`

    **Estilo obrigatório (CLAUDE.md):**
    - Single quotes em todas as strings
    - Trailing commas em todas as estruturas multi-linha
    - Imports com extensão .js (ex: `import ... from './mongo.service.js'`)
    - NestJS Logger para todos os logs
  </implementation>
</feature>

<verification>
## Verificação Geral da Fase

```bash
# Todos os testes passam (incluindo os novos TRIG-*)
pnpm test -- --testPathPattern=run-dispatch

# Suite completa sem regressões
pnpm test

# Sem erros de lint
pnpm run lint
```

Verificações manuais via grep:
```bash
# timeTrigger presente no código de produção
grep -n "timeTrigger" /root/time-trigger-api/src/dispatch/run-dispatch.service.ts

# root-level morningLimit/nightLimit NÃO aparece mais na interface VarsDoc
grep -n "morningLimit\|nightLimit" /root/time-trigger-api/src/dispatch/run-dispatch.service.ts | grep -v timeTrigger

# isAllowedDay implementado
grep -n "isAllowedDay\|allowedDays" /root/time-trigger-api/src/dispatch/run-dispatch.service.ts

# Testes TRIG presentes no spec
grep -n "TRIG-" /root/time-trigger-api/src/dispatch/run-dispatch.service.spec.ts
```
</verification>

<success_criteria>
1. `pnpm test -- --testPathPattern=run-dispatch` retorna verde (todos os testes passam)
2. `pnpm test` retorna verde (zero regressões na suite)
3. `grep "timeTrigger" src/dispatch/run-dispatch.service.ts` retorna pelo menos 5 ocorrências
4. `grep "allowedDays" src/dispatch/run-dispatch.service.ts` retorna pelo menos 2 ocorrências
5. `grep "TRIG-" src/dispatch/run-dispatch.service.spec.ts` retorna 6+ linhas (um por TRIG-01 a TRIG-06)
6. Interface VarsDoc não contém mais morningLimit/nightLimit como campos diretos (apenas dentro de TimeTriggerConfig)
7. `pnpm run lint` passa sem erros
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/05-per-client-time-controls/05-01-SUMMARY.md` com:
- O que foi implementado (interfaces novas, métodos novos/alterados)
- Decisões tomadas durante implementação
- Testes adicionados (lista dos IDs: TRIG-01 a TRIG-06)
- Qualquer desvio do plano e motivo
</output>
