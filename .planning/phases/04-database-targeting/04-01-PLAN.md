---
phase: 04-database-targeting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/database/database-scan.service.ts
  - src/database/database-scan.service.spec.ts
autonomous: true
requirements:
  - FILT-01
  - FILT-02
  - FILT-03

must_haves:
  truths:
    - "Quando TARGET_DATABASES está ausente ou é '*', todos os bancos elegíveis são processados (comportamento atual preservado)"
    - "Quando TARGET_DATABASES=sdr-4blue,dev, apenas esses dois bancos são considerados — todos os demais são ignorados antes da checagem de coleções"
    - "O filtro por nome é aplicado antes de chamar listCollections, evitando queries desnecessárias"
    - "O log estruturado mostra quantos bancos passaram ou foram excluídos pelo filtro TARGET_DATABASES"
  artifacts:
    - path: "src/database/database-scan.service.ts"
      provides: "Lógica de filtragem TARGET_DATABASES em getEligibleDatabases()"
      contains: "TARGET_DATABASES"
    - path: "src/database/database-scan.service.spec.ts"
      provides: "Testes unitários cobrindo todos os cenários de filtragem"
      exports: []
  key_links:
    - from: "src/database/database-scan.service.ts"
      to: "ConfigService"
      via: "constructor injection"
      pattern: "ConfigService"
    - from: "getEligibleDatabases"
      to: "targetFilter"
      via: "filter before listCollections loop"
      pattern: "targetDbs.*filter|filter.*targetDbs"
---

<objective>
Adiciona suporte à variável de ambiente `TARGET_DATABASES` em `DatabaseScanService.getEligibleDatabases()`.

Purpose: Permite que operadores restrinjam quais bancos de dados são processados sem alterar código — listando nomes separados por vírgula em `TARGET_DATABASES`, ou usando `*` / ausente para processar todos.

Output: `database-scan.service.ts` modificado + testes unitários cobrindo os três requisitos FILT-01, FILT-02, FILT-03.
</objective>

<execution_context>
@/root/time-trigger-api/.claude/get-shit-done/workflows/execute-plan.md
@/root/time-trigger-api/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Contrato atual de DatabaseScanService — executor deve modificar este código. -->

De src/database/database-scan.service.ts:
```typescript
import { Injectable, Logger } from '@nestjs/common';
import { MongoService } from '../mongo/mongo.service.js';

const SYSTEM_DATABASES = new Set(['admin', 'local', 'config']);
const REQUIRED_COLLECTIONS = ['runs', 'webhooks', 'vars'] as const;

@Injectable()
export class DatabaseScanService {
  private readonly logger = new Logger(DatabaseScanService.name);

  constructor(private readonly mongoService: MongoService) {}

  async getEligibleDatabases(): Promise<string[]> {
    const allDbs = await this.mongoService.listDatabaseNames();
    const clientDbs = allDbs.filter((name) => !SYSTEM_DATABASES.has(name));
    // ... loop de checagem de coleções ...
    return eligible;
  }
}
```

De src/app.module.ts:
```typescript
ConfigModule.forRoot({ isGlobal: true, envFilePath: '.env' })
// ConfigModule é global — ConfigService pode ser injetado em qualquer provider sem imports adicionais
```

ConfigService API (@nestjs/config):
```typescript
configService.get<string>('TARGET_DATABASES') // retorna string | undefined
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Aplicar filtro TARGET_DATABASES em DatabaseScanService</name>
  <files>src/database/database-scan.service.ts, src/database/database-scan.service.spec.ts</files>

  <read_first>
    - src/database/database-scan.service.ts — entender implementação atual completa
    - src/database/database-scan.service.spec.ts — entender padrão dos testes existentes (makeDb, mockMongoService, estrutura describe/it)
    - src/database/database.module.ts — verificar se ConfigService precisa ser adicionado ao módulo
  </read_first>

  <behavior>
    Novos casos de teste a adicionar no spec (manter todos os testes existentes passando):

    - TARGET_DATABASES ausente → retorna todos os bancos elegíveis (nenhum filtro aplicado)
    - TARGET_DATABASES='*' → retorna todos os bancos elegíveis (nenhum filtro aplicado)
    - TARGET_DATABASES='sdr-4blue,dev' → apenas sdr-4blue e dev passam para checagem de coleções; outros bancos nunca têm db() chamado
    - TARGET_DATABASES='sdr-4blue' (banco não existe no Mongo) → retorna array vazio (filtro aplicado, banco inexistente ignorado)
    - Log inclui contagem de bancos excluídos pelo filtro TARGET_DATABASES quando filtro está ativo
  </behavior>

  <action>
PASSO 1 — Escrever os novos testes (RED):

No describe block do spec, adicionar mock de ConfigService ao TestingModule:

```typescript
import { ConfigService } from '@nestjs/config';

// No beforeEach, adicionar ao mockMongoService o configService mock:
const mockConfigService = {
  get: jest.fn(),
};

const module: TestingModule = await Test.createTestingModule({
  providers: [
    DatabaseScanService,
    { provide: MongoService, useValue: mockMongoService },
    { provide: ConfigService, useValue: mockConfigService },
  ],
}).compile();

configService = module.get(ConfigService);
```

Os novos testes devem usar `configService.get.mockReturnValue(...)` para simular TARGET_DATABASES.

Os testes EXISTENTES devem ter `configService.get.mockReturnValue(undefined)` no beforeEach (ou dentro de cada teste) para garantir comportamento padrão "todos os bancos".

PASSO 2 — Implementar o filtro no service (GREEN):

Modificar `database-scan.service.ts`:

1. Adicionar import de ConfigService:
```typescript
import { ConfigService } from '@nestjs/config';
```

2. Injetar no constructor:
```typescript
constructor(
  private readonly mongoService: MongoService,
  private readonly configService: ConfigService,
) {}
```

3. Em `getEligibleDatabases()`, após filtrar SYSTEM_DATABASES, aplicar filtro TARGET_DATABASES ANTES do loop de listCollections:

```typescript
const targetRaw = this.configService.get<string>('TARGET_DATABASES');
const useAllDbs = !targetRaw || targetRaw.trim() === '*';

let filteredDbs: string[];
let excludedByFilter = 0;

if (useAllDbs) {
  filteredDbs = clientDbs;
} else {
  const allowList = new Set(
    targetRaw.split(',').map((s) => s.trim()).filter(Boolean),
  );
  filteredDbs = clientDbs.filter((name) => allowList.has(name));
  excludedByFilter = clientDbs.length - filteredDbs.length;
  this.logger.log(
    `TARGET_DATABASES filter: ${filteredDbs.length} allowed, ${excludedByFilter} excluded`,
  );
}
```

4. O loop de checagem de coleções deve iterar sobre `filteredDbs` (não `clientDbs`).

5. Manter o log final existente — apenas trocar `clientDbs.length` por `filteredDbs.length` se necessário para manter coerência semântica ("client DBs after filter").

CONVENÇÕES OBRIGATÓRIAS:
- Imports com extensão `.js` (nodenext): `import { ConfigService } from '@nestjs/config';` — sem extensão (pacote externo, ok)
- Single quotes em todas as strings
- Trailing comma em todos os objetos/arrays multi-linha
  </action>

  <verify>
    <automated>cd /root/time-trigger-api && pnpm run test -- --testPathPattern=database-scan --forceExit 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n 'ConfigService' /root/time-trigger-api/src/database/database-scan.service.ts` → mostra import E uso no constructor
    - `grep -n 'TARGET_DATABASES' /root/time-trigger-api/src/database/database-scan.service.ts` → mostra uso do configService.get
    - `grep -n 'allowList\|filteredDbs\|excludedByFilter' /root/time-trigger-api/src/database/database-scan.service.ts` → mostra lógica de filtragem
    - `grep -c 'TARGET_DATABASES' /root/time-trigger-api/src/database/database-scan.service.spec.ts` → valor >= 4 (múltiplos testes)
    - Todos os testes passam: `pnpm run test -- --testPathPattern=database-scan --forceExit` retorna "Tests: X passed"
    - Nenhum teste existente quebrou (os 6 testes anteriores continuam passando)
  </acceptance_criteria>

  <done>
    - ConfigService injetado no DatabaseScanService
    - TARGET_DATABASES ausente ou '*' → comportamento idêntico ao atual (FILT-02)
    - TARGET_DATABASES com lista → apenas bancos da lista são checados por coleções (FILT-01, FILT-03)
    - Log emitido quando filtro está ativo mostrando quantos bancos foram excluídos
    - Todos os testes novos e existentes passando
  </done>
</task>

</tasks>

<verification>
Verificação global da fase:

1. `pnpm run test -- --testPathPattern=database-scan --forceExit` — todos os testes passam
2. `pnpm run build` — compilação sem erros TypeScript
3. `pnpm run lint` — sem erros de lint
4. `grep -n 'ConfigService' src/database/database-scan.service.ts` — ConfigService presente
5. `grep -n 'TARGET_DATABASES' src/database/database-scan.service.ts` — filtro implementado
6. `grep -n 'filteredDbs' src/database/database-scan.service.ts` — filtro aplicado antes do loop de coleções
</verification>

<success_criteria>
- FILT-01: TARGET_DATABASES aceita '*' e lista CSV — ambos testados e funcionando
- FILT-02: Ausente ou '*' preserva comportamento atual — testado
- FILT-03: Lista CSV filtra antes da checagem de coleções — db() nunca chamado para bancos excluídos — testado
- Build TypeScript sem erros
- Zero regressões nos testes existentes
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-database-targeting/04-01-SUMMARY.md` seguindo o template em `.claude/get-shit-done/templates/summary.md`.
</output>
